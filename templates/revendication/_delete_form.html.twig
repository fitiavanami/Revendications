<form method="post" action="{{ path('app_revendication_delete', {'id': revendication.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ revendication.id) }}">
    <button class="btn">Delete</button>
</form>
