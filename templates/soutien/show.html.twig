{% extends 'base.html.twig' %}

{% block title %}Soutien{% endblock %}

{% block body %}
    <h1>Soutien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soutien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ soutien.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ soutien.email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ soutien.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_soutien_index') }}">back to list</a>

    <a href="{{ path('app_soutien_edit', {'id': soutien.id}) }}">edit</a>

    {{ include('soutien/_delete_form.html.twig') }}
{% endblock %}
